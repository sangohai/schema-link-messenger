project: schema-link-messenger
version: 1.0.0
current_phase: Phase 1 - Initialization & Infrastructure
breakpoint: 
  file: "src/api-preview.js"
  line: 1
  status: "Serverless proxy created. Moving to Frontend: bridging UI to Proxy."

phases:
  - phase: 1
    name: "Initialization & Infrastructure"
    status: completed
    tasks:
      - [x] Draft AI-Native memory protocols (.ai-skill).
      - [x] Create index.html and define 8-module file structure.
      - [x] Set up Serverless Proxy (api/parse.js) for CORS bypass.
  
  - phase: 2
    name: "Core Data Layer (GitHub API)"
    status: pending
    tasks:
      - [ ] Implement api-github.js (Base64 encode/decode, fetch, commit).
      - [ ] Implement state.js (Local synchronization).
  
  - phase: 3
    name: "UI & Interaction Layer"
    status: pending
    tasks:
      - [ ] Implement api-preview.js (Frontend call to Serverless Proxy).
      - [ ] Implement ui-board.js (Render categories and cards).
      - [ ] Implement ui-input.js (Form handling).
      - [ ] Implement ui-search.js (Real-time DOM filtering).

historical_gotchas:
  - issue: "GitHub API Rate Limiting for unauthorized requests"
    solution: "Must implement a proxy or securely inject a fine-grained scoped PAT during serverless deployment. NEVER fetch raw GitHub API directly from client without token."
  - issue: "Native JS Module strict mode"
    solution: "Always append .js extension in ES module imports. Browsers require exact file extensions."